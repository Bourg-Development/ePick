<section class="hero">
    <div class="container">
        <div class="hero-content">
            <h1><%= __('home.hero.title') %></h1>
            <p><%= __('home.hero.subtitle') %></p>
            <div class="hero-buttons">
                <a href="/auth/login" class="btn btn-primary"><%= __('home.hero.accessSystem') %></a>
                <a href="/status" class="btn btn-secondary"><%= __('home.hero.systemStatus') %></a>
            </div>
        </div>
    </div>
</section>

<section id="features" class="features">
    <div class="container">
        <div class="section-header">
            <h2><%= __('home.features.title') %></h2>
            <p><%= __('home.features.subtitle') %></p>
        </div>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">🩸</div>
                <h3><%= __('home.features.bloodAnalysis.title') %></h3>
                <p><%= __('home.features.bloodAnalysis.description') %></p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">👥</div>
                <h3><%= __('home.features.patientManagement.title') %></h3>
                <p><%= __('home.features.patientManagement.description') %></p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📊</div>
                <h3><%= __('home.features.systemReporting.title') %></h3>
                <p><%= __('home.features.systemReporting.description') %></p>
            </div>
        </div>
    </div>
</section>

<section id="quick-access" class="quick-access-section">
    <div class="container">
        <div class="section-header">
            <h2><%= __('home.quickAccess.title') %></h2>
            <p><%= __('home.quickAccess.subtitle') %></p>
        </div>
        <div class="quick-access-grid">
            <a href="/restricted/dashboard" class="access-card">
                <div class="access-icon">🏥</div>
                <h3><%= __('home.quickAccess.dashboard.title') %></h3>
                <p><%= __('home.quickAccess.dashboard.description') %></p>
            </a>
            <a href="/restricted/dashboard/analyses" class="access-card">
                <div class="access-icon">🧪</div>
                <h3><%= __('home.quickAccess.analysisWorkflow.title') %></h3>
                <p><%= __('home.quickAccess.analysisWorkflow.description') %></p>
            </a>
            <a href="/restricted/dashboard/patients" class="access-card">
                <div class="access-icon">👤</div>
                <h3><%= __('home.quickAccess.patients.title') %></h3>
                <p><%= __('home.quickAccess.patients.description') %></p>
            </a>
            <a href="/restricted/dashboard/users" class="access-card">
                <div class="access-icon">👨‍⚕️</div>
                <h3><%= __('home.quickAccess.userManagement.title') %></h3>
                <p><%= __('home.quickAccess.userManagement.description') %></p>
            </a>
        </div>
    </div>
</section>

<section class="info-boxes">
    <div class="container">
        <div class="info-grid">
            <div class="info-box">
                <h3><%= __('home.infoBoxes.gdprCompliant.title') %></h3>
                <p><%= __('home.infoBoxes.gdprCompliant.description') %></p>
            </div>
            <div class="info-box">
                <h3><%= __('home.infoBoxes.secureAudited.title') %></h3>
                <p><%= __('home.infoBoxes.secureAudited.description') %></p>
            </div>
            <div class="info-box">
                <h3><%= __('home.infoBoxes.support24h.title') %></h3>
                <p><%= __('home.infoBoxes.support24h.description') %></p>
            </div>
        </div>
    </div>
</section>

<section class="cta">
    <div class="container">
        <div class="cta-content">
            <h2><%= __('home.cta.title') %></h2>
            <p><%= __('home.cta.description') %></p>
            <a href="/auth/login" class="btn btn-primary"><%= __('home.cta.loginToSystem') %></a>
        </div>
    </div>
</section>
