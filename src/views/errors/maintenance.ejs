<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | ePick</title>
    <link rel="icon" type="image/png" href="/static/media/logos/favico.png">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        :root {
            --blood-red: #e63946;
            --dark-red: #c1121f;
            --text-black: #333333;
            --off-white: #f9f9f9;
            --light-gray: #e6e6e6;
            --medium-gray: #666666;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', 'Helvetica Neue', Helvetica, sans-serif;
        }
        
        .maintenance-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--off-white);
            padding: 20px;
        }

        .maintenance-content {
            background: white;
            border-radius: 5px;
            padding: 48px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 100%;
            border: 1px solid var(--light-gray);
        }

        .maintenance-icon {
            margin-bottom: 24px;
        }

        .maintenance-icon .material-symbols-outlined {
            font-size: 80px;
            color: var(--blood-red);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

        .maintenance-title {
            font-size: 32px;
            font-weight: bold;
            color: var(--text-black);
            margin-bottom: 16px;
        }

        .maintenance-message {
            margin-bottom: 32px;
        }

        .maintenance-message p {
            font-size: 16px;
            color: var(--medium-gray);
            line-height: 1.6;
            margin: 0;
        }

        .maintenance-details {
            background: var(--off-white);
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 32px;
            text-align: left;
            border: 1px solid var(--light-gray);
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            font-size: 16px;
        }

        .detail-item:last-child {
            margin-bottom: 0;
        }

        .detail-item strong {
            color: var(--text-black);
            font-weight: bold;
        }

        .detail-item span {
            color: var(--medium-gray);
        }

        .maintenance-actions {
            margin-bottom: 32px;
        }

        .btn {
            padding: 12px 20px;
            border-radius: 5px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            height: 48px;
            text-decoration: none;
            gap: 8px;
            font-size: 16px;
        }

        .btn-primary {
            background-color: var(--blood-red);
            color: white;
            border: none;
        }

        .btn-primary:hover {
            background-color: var(--dark-red);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(230, 57, 70, 0.3);
        }

        .maintenance-footer {
            border-top: 1px solid var(--light-gray);
            padding-top: 20px;
        }

        .maintenance-footer p {
            font-size: 16px;
            color: var(--medium-gray);
            margin: 0;
        }

        @media (max-width: 640px) {
            .maintenance-content {
                padding: 32px 24px;
            }
            
            .maintenance-title {
                font-size: 24px;
            }
            
            .maintenance-icon .material-symbols-outlined {
                font-size: 60px;
            }
        }
    </style>
</head>
<body>
    <div class="maintenance-container">
    <div class="maintenance-content">
        <div class="maintenance-icon">
            <span class="material-symbols-outlined">construction</span>
        </div>
        
        <h1 class="maintenance-title">System Under Maintenance</h1>
        
        <div class="maintenance-message">
            <p><%= message %></p>
        </div>
        
        <div class="maintenance-details">
            <div class="detail-item">
                <strong>Maintenance started:</strong>
                <span><%= enabledAt %></span>
            </div>
            <div class="detail-item">
                <strong>Initiated by:</strong>
                <span><%= enabledBy %></span>
            </div>
        </div>
        
        <div class="maintenance-actions">
            <button onclick="window.location.reload()" class="btn btn-primary">
                <span class="material-symbols-outlined">refresh</span>
                Retry
            </button>
        </div>
        
        <div class="maintenance-footer">
            <p>We apologize for the inconvenience. Please try again in a few minutes.</p>
        </div>
    </div>
    </div>

    <script>
    // Auto-refresh every 30 seconds
    setTimeout(() => {
        window.location.reload();
    }, 30000);
    </script>
</body>
</html>